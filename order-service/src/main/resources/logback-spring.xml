<?xml version="1.0" encoding="UTF-8"?>
<!--
  JSON logging for the ELK demo (Day 6 / observability).
  - "docker,elk" (OR): machine-readable JSON on stdout, shipped by logspout -> ELK
  - "!docker && !elk": Spring Boot's plain, human-readable console output (local run)
  Test profile is not docker/elk, so context-load tests keep plain output too.
-->
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProfile name="docker,elk">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="!docker &amp;&amp; !elk">
        <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>
</configuration>
